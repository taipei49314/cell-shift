# T2 — Failure Atlas（下一個長任務）

**一句話：** 把稽核包裡「記下來但沒拆開」的失敗，做成可重播的失敗地圖：誰在哪個 seed、哪個小時翻車，以及我們能不能否證一個機制。仍然不是生物學。

v1.0 已經能關題、能出收據、能剖切。長任務不是再開一層畫面，是回答：

> 這些規則什麼時候成立、什麼時候是小樣本噪音、什麼時候是艙的結構極限？

## 為什麼是這個，不是別的

稽核包 `18058ca` 已經凍結三件 **已知失敗**：

| 對照 | 翻車 seed | 狀態 |
| --- | --- | --- |
| 低黏著 r90 > 高黏著 | **99** | 留下，未解釋 |
| C1 加速週期佔比 > C2 | **99** | 留下，未解釋 |
| 高運動 r90 > 低運動 | **2026** | 留下，未解釋 |

另外兩件也是 UNKNOWN：

- 氧氣掃描的 **o2Drop 不單調**（只敢鎖兩端壞死）
- 預設協議活細胞卡在約 **10–15**（接觸抑制 14），所以「組織」其實很小

下一刀若去做營養場、血管、或 GitHub Pages，是換題，不是把這題做完。

## 範圍

**In**

1. **Seed atlas** — 三個翻車對照各跑固定種子表 `0..31`（32 個，不是 5 個）。產出 hold 率。不准丟掉失敗 seed。
2. **99 / 2026 解剖** — 對翻車 seed 出時間序列（每 20 h：r90、live、necrotic、clone share），對上一個成立 seed。問：是 n 太小、是過早壞死、還是方向真的反了。
3. **氧氣曲面** — env O₂ ∈ {0.35, 0.45, 0.55, 0.70, 0.90} × t ∈ {80, 160, 240}。標出 o2Drop 非單調的格子。只鎖我們量到的，不編故事。
4. **生長天花板** — 同 seed，`contactLimit` 14 vs 26，live(t)。鎖一條 Measured：預設接觸抑制下活細胞飽和在一個區間。
5. **機制假說預註冊** — 在看 32-seed 全表之前寫進 ledger。看完只能判 PASS / FAIL / UNKNOWN，禁止事後改題幹。
6. **T2 稽核包** — `npm run audit` 再打一包，含 atlas 與解剖。

**Out**

- 改預設協議參數讓 99「變綠」
- 從測試名單刪除失敗 seed
- 新 3D、體積光、部署網站
- 營養場 / 免疫 / 血管 / PhysiCell 對齊
- 宣稱「找到了生物機制」

## 預註冊機制（現在寫死，T2 開始前不得改）

寫進 `src/sim/t2-hypotheses.ts`，開跑前凍結：

| ID | 題幹 | 否證 |
| --- | --- | --- |
| H99-smalln | seed 99 的黏著對照翻車，是因為 live < 8，r90 被少數細胞支配 | 若 99 在 live ≥ 15 時對照仍反號 → FAIL |
| H99-early | 99 在 t≤80 就反號，不是 160 h 才壞 | 若 80 h 順、160 h 才反 → FAIL |
| H2026-pack | 2026 高運動 r90 較小，是因為散開後更易缺氧縮小活體 | 若高運動的 necroticFrac **低於** 低運動且 r90 仍較小 → FAIL |
| Hox-mid | o2Drop 在中段 O₂（0.5–0.7）低於兩端 | 若中段格子處處 ≥ 兩端最小 o2Drop → FAIL |
| Hcap | contactLimit=14 時 160 h live ∈ [6, 20]（五個舊 seed） | 任一種子落在區間外 → FAIL |

判不成就標 UNKNOWN，不要換題幹去湊 PASS。

## 分期（長，可斷）

| 段 | 產出 | 出口 |
| --- | --- | --- |
| T2.1 Atlas | `artifacts/t2/atlas-32.json` + 測試：32 列齊、失敗列仍在 | `npm test` 含 atlas 列數 = 32 |
| T2.2 解剖 | `artifacts/t2/seed-99.json`、`seed-2026.json` 時間序列 | 每個 H99-* / H2026-* 有判決 |
| T2.3 氧氣曲面 | `artifacts/t2/oxygen-grid.json` | Hox-mid 有判決 |
| T2.4 天花板 | `artifacts/t2/ceiling.json` | Hcap 有 Measured 列 |
| T2.5 包 | 新稽核包上桌面 | `npm run audit` 綠，AUDIT.md 寫 T2 判決 |

一段沒綠，不准進下一段。

## 怎樣叫這次長任務結束

全部成立：

1. 32-seed atlas 可 `npm run chamber -- t2-atlas` 重產。
2. 五條 T2 假說都有 PASS / FAIL / UNKNOWN，沒有空白。
3. CLAIMS 只加 Measured / 已知失敗，Not-claimed 三行原樣。
4. 預設 Hypoxic / Intact / Invasive 的舊鎖仍然全綠。
5. 打出 `cell-shift-audit-<sha>.zip`，封面寫 T2，不寫「癌症研究完成」。

不打 v2.0。這是 v1 上面的研究附錄，不是新產品。

## 工時與跑法

這是 **數日級**（32 seed × 3 對照 × 160 h + 時間序列 + 氧氣網），不是一晚能假裝做完。

- 本機：`npm run chamber -- t2-atlas` 可分片（先 0–7，再 8–15…）。
- 無人駐守：只准按 T2.1 → T2.5 順序，讀本檔，禁止碰 `TissueChamber.tsx`。兩輪沒有新 artifact 就停。
- 未下令「開工 T2」或「開駐守」前，**不開排程**。

## 對抗自己

若 32-seed 之後 99 只是 5 顆種子裡的運氣，H99-smalln 會 PASS，atlas hold 率會接近 1——那 T2 的結論是「5-seed 表過度解讀」，不是「99 是特殊動力學」。兩種都算完成，只要寫在判決裡。
